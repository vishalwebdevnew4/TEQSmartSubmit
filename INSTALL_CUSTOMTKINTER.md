# Install CustomTkinter

To use the modern GUI, install CustomTkinter:

```bash
pip install customtkinter pillow
```

Or use the requirements file:

```bash
pip install -r requirements.txt
```

Then run:

```bash
python run_modern.py
```


